# OS42 Orchestrator Dependencies
#
# Pinned to what this repo's whole test suite has actually been run
# against (see run_all_tests.py / .github/workflows/tests.yml), not
# whatever was current when a given package was first added.

# API Framework
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2

# HTTP Client
httpx==0.28.1

# Logging
structlog==26.1.0

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0

# Development
python-dotenv==1.2.2
